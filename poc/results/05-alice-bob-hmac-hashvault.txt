================================================================================
  VS3 PROXY — BIDIRECTIONAL MESSAGING WITH HMAC ROTATING SENTINEL
  Pool: pool.hashvault.pro:3333 (Monero, standard unmodified pool)
  Date: 2026-03-30T16:00:00Z
  Proxy: VS3 proxy with HMAC ghost tag (Appendix D)
================================================================================

TEST SETUP
  - VS3 Proxy with HMAC enabled (salt: "test-salt-2026")
  - Upstream: pool.hashvault.pro:3333 (standard Monero pool, no VS3 awareness)
  - Alice wallet: 44AFFq5kSiGBoZ... (pass: alice-secret-2026)
  - Bob wallet: 48Y4SoUJM5L3YX... (pass: bob-secret-2026)
  - HMAC key derivation: HKDF-SHA256(miner_pass, pool_salt, "tnzx-ghost-v1")
  - Ghost sentinel: HMAC-SHA256(session_key, nonce[1..3])[0] per share

HMAC KEY DERIVATION
  Alice: session_key = HKDF-SHA256("alice-secret-2026", "test-salt-2026", "tnzx-ghost-v1")
  Bob:   session_key = HKDF-SHA256("bob-secret-2026", "test-salt-2026", "tnzx-ghost-v1")

PHASE 1: MINING GATE ACTIVATION
  Alice sends 3 real shares → forwarded to pool → Mining Gate ACTIVE
  Bob sends 3 real shares → forwarded to pool → Mining Gate ACTIVE
  Total real shares forwarded to pool: 6

PHASE 2: ALICE → BOB (HMAC ghost shares)
  Message: "I am safe. Meet me at the bridge."
  VS3 Frame: 41 bytes (8B header + 33B payload)
  Ghost shares: 9

  Sentinel bytes (nonce[0]): 0x5d, 0x58, 0x9f, 0xf5, 0x04, 0x09, 0x45, 0x39, 0xcc
  None equal 0xAA. Each derived from HMAC(alice_key, nonce[1..3]).
  Statistically indistinguishable from random.

  Result: frame assembled, routed to Bob → EXACT MATCH ✓

PHASE 3: BOB → ALICE (HMAC ghost shares)
  Message: "Tomorrow, 6pm. Stay hidden."
  VS3 Frame: 35 bytes (8B header + 27B payload)
  Ghost shares: 7

  Sentinel bytes (nonce[0]): 0x27, 0xd3, 0xc4, 0xe6, 0x99, 0xe1, 0xa4
  None equal 0xAA. Each derived from HMAC(bob_key, nonce[1..3]).

  Result: frame assembled, routed to Alice → EXACT MATCH ✓

RESULTS
================================================================================

  Alice → Bob: "I am safe. Meet me at the bridge."  →  EXACT MATCH ✓
  Bob → Alice: "Tomorrow, 6pm. Stay hidden."         →  EXACT MATCH ✓

  Real shares forwarded to pool:    6
  Ghost shares intercepted:         16 (9 + 7)
  Ghost shares leaked to pool:      0
  VS3 frames assembled:             2
  Nonce bytes starting with 0xAA:   0 (HMAC eliminates fixed sentinel)
  Unique sentinel values:           16 out of 16

  DPI ANALYSIS: An observer monitoring the Stratum connection sees 22 share
  submissions (6 real + 16 ghost). The ghost shares have nonce[0] values that
  are uniformly distributed — no statistical pattern distinguishes them from
  real shares at the DPI level. Only an adversary who verifies PoW validity
  of every share can identify ghost shares (inherent to the ghost concept).

  The HMAC rotating sentinel (Appendix D of the Visual Stratum paper)
  eliminates the cheapest detection path: pattern matching on nonce[0] = 0xAA.

================================================================================
